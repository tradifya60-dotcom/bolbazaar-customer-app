{
  "name": "bolbazaar-customer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No frontend build. Using dist/index.html redirect'",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "typescript": "^5.6.3"
  }
}